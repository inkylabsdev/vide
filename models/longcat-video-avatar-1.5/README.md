# longcat-video-avatar-1.5 (Cog package)

This directory contains the local Cog package used by `vide animate-avatar`.

## Start the server

```console
$ cd models/longcat-video-avatar-1.5
$ cog serve
```

The CLI assumes this HTTP server is running (default: `http://127.0.0.1:5000`).
